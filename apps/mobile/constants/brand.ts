/**
 * Brand naming.
 *
 * The assistant's name is PROVISIONAL — pending a trademark/domain/social
 * check (ficha técnica §33 lists it as an open risk). It was "Vera" in the
 * design prototype and the ficha; UVA changed it to "Abril" while that check
 * runs. Keep every user-facing mention going through this constant so the next
 * rename is a one-line change, not another sweep across the codebase.
 */
export const ASSISTANT_NAME = 'Abril';
